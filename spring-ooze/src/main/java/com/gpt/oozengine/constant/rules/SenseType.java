package com.gpt.oozengine.constant.rules;

/** Special senses. Passive Perception lives on the stat block itself, not here,
 * because it is a derived score rather than a sense with a radius. */
public enum SenseType {
  BLINDSIGHT,
  DARKVISION,
  TREMORSENSE,
  TRUESIGHT
}
