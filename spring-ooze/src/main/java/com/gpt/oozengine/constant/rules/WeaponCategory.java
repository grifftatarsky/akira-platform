package com.gpt.oozengine.constant.rules;

/** The four weapon table sections, which together encode proficiency group and
 * whether the weapon is used at range. */
public enum WeaponCategory {
  SIMPLE_MELEE,
  SIMPLE_RANGED,
  MARTIAL_MELEE,
  MARTIAL_RANGED
}
