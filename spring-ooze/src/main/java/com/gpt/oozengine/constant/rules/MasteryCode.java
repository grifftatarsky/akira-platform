package com.gpt.oozengine.constant.rules;

/** The eight weapon mastery properties, as a behavioural handle. Same split as
 * {@link ConditionCode}: the table owns the text, the enum owns the behaviour. */
public enum MasteryCode {
  CLEAVE,
  GRAZE,
  NICK,
  PUSH,
  SAP,
  SLOW,
  TOPPLE,
  VEX
}
